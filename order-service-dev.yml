server:
  port: 8083

spring:
  datasource:
    url: jdbc:h2:mem:orderdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


test:
  message: this is from order


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
